# Quarkus HTTP configuration
quarkus:
  application:
    name: backend-gateway-proxy
  index-dependency:
    bfa-library:
      group-id: com.agent
      artifact-id: bfa-library
  http:
    host: 0.0.0.0
    port: ${PORT:8080}
  log:
    level: INFO
    category:
      "com.agent.gateway.proxy":
        level: DEBUG
    console:
      format: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
  smallrye-openapi:
    always-run-filter: true
  
  # Health and Metrics
  smallrye-health:
    ui:
      enable: true
  micrometer:
    enabled: true
    export:
      prometheus:
        enabled: true
        path: /q/metrics
  container-image:
    builder: jib
    build: false
    push: false
    registry: ${CONTAINER_IMAGE_REGISTRY:}
    group: ${CONTAINER_IMAGE_GROUP:}
    name: ${CONTAINER_IMAGE_NAME:${quarkus.application.name}}
    tag: ${CONTAINER_IMAGE_TAG:latest}
  jib:
    base-jvm-image: eclipse-temurin:21-jre
    ports:
      - ${PORT:8080}
    use-current-timestamp: true

# Gateway configuration
gateway:
  # Auth service configuration
  auth:
    enabled: true
    service-url: http://localhost:8081              # Auth service base URL
    session-id-header: X-Session-Id                 # Header name for session ID
    session-id-cookie: sessionId                    # Cookie name for session ID
    timeout: 5s
    # security-type: cloudrun                      # Optional Cloud Run IAM auth for private auth services
    # security-config:
    #   audience: https://oauth-service-abcde-ew.a.run.app/
    # tls-profile: internal-ca                    # Optional named TLS profile for HTTPS auth services

  # Optional outbound TLS profiles for auth service and backend calls
  tls:
    profiles: {}
    # profiles:
    #   internal-ca:
    #     truststore:
    #       path: /secrets/truststore.p12
    #       password: ${TRUSTSTORE_PASSWORD:changeit}
    #       type: PKCS12
    #   mutual-tls:
    #     truststore:
    #       path: /secrets/truststore.p12
    #       password: ${TRUSTSTORE_PASSWORD:changeit}
    #       type: PKCS12
    #     keystore:
    #       path: /secrets/client-keystore.p12
    #       password: ${KEYSTORE_PASSWORD:changeit}
    #       key-password: ${KEY_PASSWORD:${KEYSTORE_PASSWORD:changeit}}
    #       type: PKCS12
  
  # Schema validation settings
  schemas:
    directory: classpath:schemas/
    validate-requests: true        # Enable path/method validation
    validate-bodies: true           # Enable request body validation (disable to skip body checks)
    validate-responses: false       # Trim JSON responses to the schema-defined contract
    strict-mode: true               # Reject if no schema found

  resources:
    order-summary:
      schema: order-summary.yaml
      orders-backend: orders-service
      orders-path-template: /details/{id}
      payments-backend: payments-service
      payments-path-template: /orders/{id}
    
  # Backend configurations (file-based - NO database!)
  backends:
    # Example: JWT authentication
    - name: example-service
      baseUrl: http://localhost:9001
      path: /api/v1/example
      schema: example-service.yaml
      timeout: 30s
      enabled: true
      http-version: http1_1
      securityType: jwt
      # proxy:
      #   host: corp-proxy.internal
      #   port: 8080
      #   non-proxy-hosts:
      #     - localhost
      #     - 127.0.0.1
      #     - "*.svc.cluster.local"
      # tls-profile: internal-ca
      auth-request:
        sparte-gvo:
          - a
          - b
        btx:
          - FirstFunction
        pss:
          - SecondFunction
      # Any of the auth-request fields above can be omitted when not needed.
      # securityConfig:
      #   bearer-source: customer_access_token
      #   static-headers.x-api-key: ${APIGEE_API_KEY}
      #
      # Optional auth path override:
      # auth-request:
      #   path: /custom-auth/tokens/{sessionId}
      #   btx:
      #     - FirstFunction
      #
      # Optional JWT authz flow:
      # authz-request:
      #   path: /auth/authz/eidp/{sessionId}
      #   branch-customer-number-source: header:Branch-Customer-Number
      #   gvo-entitlements-list:
      #     - entitlement-a
      #   service-shop-transactions:
      #     - shop-a
      # securityConfig:
      #   token-headers.X-Authorization-Token: authorization_token
      #   token-headers.X-Eidp-Access-Token: eidp_access_token

    # Example: JWT auth for Glue-style backends
    # - name: glue-service
    #   baseUrl: http://localhost:9007
    #   path: /api/v1/glue
    #   schema: glue-service.yaml
    #   timeout: 30s
    #   enabled: true
    #   securityType: jwt
    #   auth-request:
    #     sparte-gvo:
    #       - a
    #       - b
    #     btx:
    #       - FirstFunction
    #     pss:
    #       - SecondFunction
    #   securityConfig:
    #     bearer-source: auth_z_token
    #     token-headers.X-Glue-Token: glue_token

    # Example: Form-encoded auth-service request
    # - name: form-service
    #   baseUrl: http://localhost:9008
    #   path: /api/v1/form
    #   schema: form-service.yaml
    #   timeout: 30s
    #   enabled: true
    #   securityType: form
    #   securityConfig:
    #     mode: auth-service
    #     auth-path: /auth/form
    #     form-params.grant_type: literal:client_credentials
    #     form-params.client_id: header:Client-Id
    #     form-params.client_secret: cookie:clientSecret
    #     form-params.scope: literal:appointments.read
    #     response-headers.Authorization: access_token
    #     response-header-prefixes.Authorization: Bearer
    #     response-headers.X-Tenant-Token: tenant_token

    # Example: Inline form auth on the actual backend request
    # - name: form-inline-service
    #   baseUrl: http://localhost:9009
    #   path: /api/v1/form-inline
    #   schema: form-inline-service.yaml
    #   timeout: 30s
    #   enabled: true
    #   securityType: form
    #   securityConfig:
    #     mode: inline
    #     form-params.grant_type: literal:client_credentials
    #     form-params.client_id: header:Client-Id
    #     form-params.client_secret: cookie:clientSecret
    #   # Requests to this backend must use:
    #   # Content-Type: application/x-www-form-urlencoded
      
    # Example: Basic authentication
    # - name: legacy-service
    #   baseUrl: http://localhost:9003
    #   path: /api/v1/legacy
    #   schema: legacy-service.yaml
    #   timeout: 30s
    #   enabled: true
    #   securityType: basic
    #   tls-profile: mutual-tls
    #   securityConfig:
    #     username: ${LEGACY_SERVICE_USERNAME:admin}
    #     password: ${LEGACY_SERVICE_PASSWORD:secret}

    # Example: Cloud Run service-to-service authentication
    # - name: cloudrun-service
    #   baseUrl: https://orders-service-abcde-ew.a.run.app
    #   path: /
    #   schema: orders-service.yaml
    #   timeout: 30s
    #   enabled: true
    #   http-version: http2
    #   securityType: cloudrun
    #   securityConfig:
    #     audience: https://orders-service-abcde-ew.a.run.app/
    
    # Example: No authentication
    # - name: public-service
    #   baseUrl: http://localhost:9004
    #   path: /api/v1/public
    #   schema: public-service.yaml
    #   timeout: 30s
    #   enabled: true

    # Example: Multi-backend resource dependencies
    # - name: orders-service
    #   baseUrl: http://localhost:9005
    #   path: /orders
    #   schema: orders-service.yaml
    #   enabled: true
    #   securityType: none
    # - name: payments-service
    #   baseUrl: http://localhost:9006
    #   path: /payments
    #   schema: payments-service.yaml
    #   enabled: true
    #   securityType: none
