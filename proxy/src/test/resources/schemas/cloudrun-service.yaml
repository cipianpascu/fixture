openapi: 3.0.0
info:
  title: Cloud Run Service API
  version: 1.0.0

paths:
  /ping:
    get:
      operationId: pingCloudRun
      responses:
        '200':
          description: cloud run auth echo
          content:
            application/json:
              schema:
                type: object
                properties:
                  serverlessAuthorization:
                    type: string
