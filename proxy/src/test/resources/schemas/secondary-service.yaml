openapi: 3.0.0
info:
  title: Secondary Service API
  version: 1.0.0

paths:
  /ping:
    get:
      operationId: ping
      responses:
        '200':
          description: pong
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
